# Notebooks

Exploratory notebooks may live here. **Do not commit outputs.** Clear all cell outputs before committing.

Prefer scripts under `src/driftbound/experiments/` for reproducible results written to `generated-results/` (gitignored).
